# react-native-background-tasks-native
Project being code in React Native + Android(Java) to processing the background task using WorkerManager and trigger event back to javascript code using Headless JS.
