module.exports = async taskData => {
  // We get the data passed to bundle from native here: taskData, or call APIs
  
  var myHeaders = new Headers();
  myHeaders.append('Content-Type', 'application/x-www-form-urlencoded');

  var urlencoded = new URLSearchParams();
  urlencoded.append('email', 'admin_rental@gmail.com');
  urlencoded.append('password', 'admin');

  var requestOptions = {
    method: 'POST',
    headers: myHeaders,
    body: urlencoded,
    redirect: 'follow',
  };

  fetch('http://192.168.1.11:3000/auth/v1/login', requestOptions)
    .then(response => response.text())
    .then(result => console.log(result))
    .catch(error => console.log('error', error));
  console.log(
    'Hey this is a headless JS task call to fetch data from Circular APIs ',
    taskData,
  );
  console.log(
    'Hey this is a headless JS task call to fetch data from Circular APIs ',
    taskData,
  );
  return;
};
